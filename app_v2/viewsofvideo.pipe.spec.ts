import { ViewsofvideoPipe } from './viewsofvideo.pipe';

describe('ViewsofvideoPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewsofvideoPipe();
    expect(pipe).toBeTruthy();
  });
});
